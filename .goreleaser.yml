# generated by: yake code goreleaser
version: 2
before:
  hooks:
    - go mod tidy
builds:
  - id: systemd-supervisord
    binary: systemd-supervisord
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
nfpms:
  - id: systemd-supervisord
    package_name: systemd-supervisord
    builds:
      - systemd-supervisord
    vendor: vitalvas
    homepage: https://github.com/vitalvas/systemd-supervisord
    description: Supervisor daemon for systemd services and timers
    license: MIT
    formats:
      - deb
    bindir: /usr/bin
    contents:
      - src: misc/systemd-supervisord.service
        dst: /lib/systemd/system/systemd-supervisord.service
    scripts:
      postinstall: misc/postinstall.sh
checksum:
  name_template: checksums.txt
  algorithm: sha256
snapshot:
  version_template: '{{ .Version }}-{{ .CommitTimestamp }}-{{ .ShortCommit }}'
changelog:
  disable: true
release:
  github:
    owner: vitalvas
    name: systemd-supervisord
  draft: false
  prerelease: auto
  mode: keep-existing
